{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "poetry run langchain serve --host 0.0.0.0 --port $PORT"
    }
}